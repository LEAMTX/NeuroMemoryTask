using UnityEngine;

public class MemoryManager : MonoBehaviour
{
   private void Start()
    {
        Debug.Log("Memory task started");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
